---
import { t, withSlash, type Lang } from '../i18n/translations';
import { references } from '../data/references';

interface Props {
  lang?: Lang;
}

const { lang = 'tr' } = Astro.props;
---

<section class="relative section-padding overflow-hidden noise-overlay" id="references">
  <div class="absolute inset-0 bg-primary-dark"></div>

  <div class="relative z-10 container-custom">
    <div class="text-center max-w-2xl mx-auto mb-16" data-animate="fade-up">
      <div class="section-label justify-center">{{tr:'İş Ortaklarımız', en:'Our Partners', de:'Unsere Partner', ar:'شركاؤنا'}[lang]}</div>
      <h2 class="text-3xl sm:text-4xl lg:text-[2.75rem] font-heading font-bold text-white mb-4">
        {t(lang, 'references.title')}
      </h2>
      <p class="text-text-light text-base leading-relaxed">{t(lang, 'references.subtitle')}</p>
    </div>

    <!-- Marquee (desktop) -->
    <div class="ref-marquee-wrap" data-animate="fade-up" data-delay="200">
      <div class="ref-marquee">
        {[...references, ...references].map((ref) => (
          <div class="ref-item">
            <img src={ref.image} alt={ref.name} class="ref-logo" loading="eager" />
          </div>
        ))}
      </div>
    </div>

    <!-- Static grid (mobile) -->
    <div class="ref-grid" data-animate="fade-up" data-delay="200">
      {references.map((ref) => (
        <div class="ref-item">
          <img src={ref.image} alt={ref.name} class="ref-logo" loading="eager" />
        </div>
      ))}
    </div>

    <div class="text-center mt-12" data-animate="fade-up">
      <a
        href={withSlash({tr:'/referanslar', en:'/en/references', de:'/de/references', ar:'/ar/references'}[lang])}
        class="inline-flex items-center gap-2 text-accent/60 text-sm font-mono uppercase tracking-wider hover:text-accent hover:gap-3 transition-all"
      >
        {t(lang, 'references.all')}
        <svg class="w-4 h-4" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M17 8l4 4m0 0l-4 4m4-4H3"/></svg>
      </a>
    </div>
  </div>
</section>

<style>
  .ref-marquee-wrap {
    display: none;
    overflow: hidden;
    mask-image: linear-gradient(to right, transparent, black 8%, black 92%, transparent);
    -webkit-mask-image: linear-gradient(to right, transparent, black 8%, black 92%, transparent);
  }
  @media (min-width: 640px) {
    .ref-marquee-wrap { display: block; }
    .ref-grid { display: none !important; }
  }

  .ref-marquee {
    display: flex;
    gap: 1.5rem;
    width: max-content;
    animation: refMarquee 40s linear infinite;
  }
  .ref-marquee:hover {
    animation-play-state: paused;
  }
  @keyframes refMarquee {
    0% { transform: translateX(0); }
    100% { transform: translateX(-50%); }
  }

  .ref-item {
    flex-shrink: 0;
    width: 220px;
    height: 100px;
    background: #fff;
    border-radius: 4px;
    overflow: hidden;
    padding: 1rem;
    transition: all 0.4s ease;
  }
  .ref-item:hover {
    transform: translateY(-2px);
    box-shadow: 0 8px 32px rgba(200, 162, 60, 0.15);
  }

  .ref-logo {
    width: 100%;
    height: 100%;
    object-fit: contain;
    opacity: 0.9;
    transition: all 0.4s ease;
  }
  .ref-item:hover .ref-logo {
    opacity: 1;
  }

  /* Mobil: dik ekranda 2 sütun, 480px üstünde 3 sütun. minmax(0,1fr) img'nin
     min-content katkısıyla sütunun büyümesini engeller. */
  .ref-grid {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 0.75rem;
  }
  @media (min-width: 480px) {
    .ref-grid { grid-template-columns: repeat(3, minmax(0, 1fr)); }
  }
</style>
